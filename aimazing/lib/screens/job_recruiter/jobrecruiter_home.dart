import 'package:flutter/material.dart';

class RecruiterHome extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Recruiter Dashboard'),
      ),
      body: Center(
        child: Text('Welcome Recruiter!'),
      ),
    );
  }
}
